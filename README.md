# Sonics Server Control

Console-first DayZ server configuration tools for Cloudflare Pages.

The project intentionally has no RCON component. It provides browser-only editing, validation, map planning, and export tools compatible with the Nitrado console file workflow.
