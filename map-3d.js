import * as THREE from 'https://cdn.jsdelivr.net/npm/three@0.181.0/build/three.module.js';import {OrbitControls} from 'https://cdn.jsdelivr.net/npm/three@0.181.0/examples/jsm/controls/OrbitControls.js';
const root=document.querySelector('#scene'),scene=new THREE.Scene(),camera=new THREE.PerspectiveCamera(55,innerWidth/(innerHeight-60),.1,500),renderer=new THREE.WebGLRenderer({antialias:true}),controls=new OrbitControls(camera,renderer.domElement),ray=new THREE.Raycaster(),pointer=new THREE.Vector2(),objects=[];renderer.setSize(innerWidth,innerHeight-60);renderer.setPixelRatio(devicePixelRatio);root.prepend(renderer.domElement);scene.background=new THREE.Color('#102943');camera.position.set(55,55,55);controls.target.set(0,0,0);controls.enableDamping=true;scene.add(new THREE.HemisphereLight(0xbfeaff,0x172712,2),new THREE.DirectionalLight(0xffffff,2));scene.children.at(-1).position.set(30,50,20);let texture=new THREE.TextureLoader().load('maps/chernarusplus.png');texture.colorSpace=THREE.SRGBColorSpace;let terrain=new THREE.Mesh(new THREE.PlaneGeometry(100,100,80,80),new THREE.MeshStandardMaterial({map:texture,roughness:1}));terrain.rotation.x=-Math.PI/2;scene.add(terrain);let selected=null;function render(){document.querySelector('#list').innerHTML=objects.map((o,i)=>`<button class="item ${o===selected?'active':''}" data-i="${i}">${o.userData.name}<br><small>x ${o.position.x.toFixed(1)} z ${o.position.z.toFixed(1)}</small></button>`).join('');document.querySelectorAll('[data-i]').forEach(b=>b.onclick=()=>{selected=objects[+b.dataset.i];document.querySelector('#class').value=selected.userData.name;render()})}function add(point){let name=document.querySelector('#class').value.trim()||'Land_Wall_Gate_FenR',mesh=new THREE.Mesh(new THREE.BoxGeometry(2,2,2),new THREE.MeshStandardMaterial({color:0x35bfff,transparent:true,opacity:.85}));mesh.position.copy(point);mesh.position.y=1;mesh.userData={name};scene.add(mesh);objects.push(mesh);selected=mesh;render()}renderer.domElement.addEventListener('click',e=>{pointer.x=e.offsetX/renderer.domElement.clientWidth*2-1;pointer.y=-(e.offsetY/renderer.domElement.clientHeight)*2+1;ray.setFromCamera(pointer,camera);let hit=ray.intersectObject(terrain)[0];if(hit)add(hit.point)});document.querySelector('#class').onchange=()=>{if(selected){selected.userData.name=document.querySelector('#class').value;render()}};document.querySelector('#export').onclick=()=>{let data={Objects:objects.map(o=>({name:o.userData.name,pos:[Math.round((o.position.x+50)/100*15360),o.position.y,Math.round((50-o.position.z)/100*15360)],ypr:[0,0,o.rotation.y],scale:1}))},a=document.createElement('a');a.href=URL.createObjectURL(new Blob([JSON.stringify(data,null,2)],{type:'application/json'}));a.download='sonics-3d-objects.json';a.click()};addEventListener('resize',()=>{camera.aspect=innerWidth/(innerHeight-60);camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight-60)});(function loop(){requestAnimationFrame(loop);controls.update();renderer.render(scene,camera)})();
